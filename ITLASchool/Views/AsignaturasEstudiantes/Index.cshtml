@model IEnumerable<ITLASchool.Models.AsignaturasEstudiantes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asignaturas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estudiantes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asignaturas.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estudiantes.Apellido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AsignaturasEstudiantesID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AsignaturasEstudiantesID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AsignaturasEstudiantesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
